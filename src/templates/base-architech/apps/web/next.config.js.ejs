/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  transpilePackages: ["@<%= projectName %>/ui", "@<%= projectName %>/db", "@<%= projectName %>/auth"],
};

module.exports = nextConfig; 