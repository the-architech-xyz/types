/**
 * <%= packageName.charAt(0).toUpperCase() + packageName.slice(1) %> Package
 * 
 * This package will be configured by The Architech <%= packageName.charAt(0).toUpperCase() + packageName.slice(1) %> Agent.
 */

export default {}; 